# About
Raw rust bindings to [ZenKitCAPI](https://github.com/GothicKit/ZenKitCAPI) library for loading Gothic 1/2 files.


# Building
Bindings are automatically generated using bindgen.
Repository needs to be recursively cloned for generation to work. 

```bash
./build.sh
```
